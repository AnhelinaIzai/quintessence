
<%= form_for(@test_question, url: course_test_questions_path) do |f| %>
  <% if @test_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_question.errors.count, "error") %> prohibited this test_question from being saved:</h2>

      <ul>
      <% @test_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :course_id, value: @course.id %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text, :size => "50x3" %>
  </div>

  <div class="actions">
    <%= f.submit :class =>"subm"%>
  </div>
<% end %>
